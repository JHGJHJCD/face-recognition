# סקירת באגים כללית — 22/9/2026 (v1.8)

נסרקו: `webui/backend.py`, `webui/static/app.js`, `core/db.py`, `core/jobs.py`, `core/live.py`, `core/updater.py`, `core/reports.py`.
סטטוס: ✅ = תוקן ב-v1.9 · ⬜ = פתוח (לתקן דרך `/בודק-באגים <נקודה>`).

| # | חומרה | מה קורה | איפה | סטטוס |
|---|---|---|---|---|
| 1 | גבוה · נתונים | שני אנשים שונים עם **אותו שם** מתמזגים בשקט לאדם אחד (וגם תאריך לידה/הערות נדרסים); `people/update` מאפשר שם כפול | `db.get_or_create`, `backend.py` people/update | ⬜ דורש הכרעת UX (לשאול "זה אותו אדם?") |
| 2 | גבוה · זיהוי | `reload_gallery` מחליף `g_ids/g_embs/g_index` בלי נעילה → `IndexError` נדיר בסריקה/rematch; ב-rematch משאיר עדכונים חלקיים | `db.py:100-106` | ⬜ להשים tuple אחד אטומית |
| 3 | בינוני · נתונים | "תן שם" בבדיקת סרטון אחרי סריקה חדשה — `S.videoSel` ישן → רושם אדם אחר | `app.js videoSync` | ✅ מאופס כשמתחילה סריקה |
| 4 | בינוני · זיהוי | שינוי גלריה בזמן rematch → לא מתוזמן rematch נוסף | `backend.gallery_changed` | ⬜ דגל dirty |
| 5 | בינוני · יציבות | `rematch_photos` מחזיק `db.lock` לאורך כל הלולאה — הממשק והנוכחות נתקעים | `db.py:480` | ⬜ לחשב מחוץ לנעילה, לכתוב במנות |
| 6 | בינוני · יציבות | `camera/pause` — שתי בקשות במקביל עלולות להשאיר מצלמה מושהית | `app.js modal()` | ✅ ההחזרה מחכה להשהיה |
| 7 | בינוני · UX | רישום מהמצלמה לא פג אם אין פנים → "רושם…" לנצח ומשתיק התרעות/AI | `live._enroll_step` | ✅ פסק-זמן לפני בדיקת פנים |
| 8 | בינוני · UX | `S.rev` מתעדכן גם כשחלון פתוח → שינוי שקרה בזמן החלון לא נטען | `app.js pollLoop` | ✅ `S.rev` לא מתעדכן מאחורי חלון |
| 9 | בינוני · נתונים | `merge_persons`/`attendance/add` לא מאמתים שהיעד קיים → רשומות יתומות | `db.py:181`, `backend.py:998` | ⬜ |
| 10 | נמוך · נתונים | `settings.json` ו-`current.txt` נכתבים לא-אטומית (קריסה → ברירות מחדל) | `db.Settings.save`, `updater` | ⬜ `.tmp` + `os.replace` |
| 11 | נמוך · נתונים | `restore`/`clear("all")` לא מחכים ל-rematch רץ; `clear` לא מאפס `video_result` | `backend.py:668-702,1016` | ⬜ |
| 12 | נמוך · זיהוי | אחרי החלפת מודל זהות, תמונות שנסרקו לא נסרקות מחדש (`photo_known` בלי `model`) | `db.photo_known` | ⬜ |
| 13 | נמוך · UX | תמונות פגומות בסריקה נבלעות בלי דיווח | `jobs.py:49` | ⬜ |
| 14 | נמוך · UX | Release אחרון בלי `app.zip` → "אתה מעודכן" במקום שגיאה | `updater.check_latest` | ⬜ |
| 15 | נמוך · דיוק | רישום מהמצלמה עלול לשמור וקטור בלי TTA (הניתוח כבר הטמיע `tta=False`) | `live.py:122` | ⬜ תמיד `tta=True` ברישום |

נבדק ותקין: `esc()` על כל נתוני innerHTML; handlers בודקים קיום אלמנט; `Net.__call__` נעול; `save_photo`/`backup_to` עקביים;
מיגרציות DB; `stop_flag` בבדיקת סרטון; `X-Meta` עברית; `is_newer`/ETag; `restore` עם עותק ביטחון.
